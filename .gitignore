# Critical
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test & emulator artifacts
test-results
playwright-report
.firebase

# Agent / tool config & artifacts (kept local per project decision)
.cursor/
.agents/
.playwright-mcp/
.superpowers/
CLAUDE.md
skills-lock.json

# Local Firebase project binding (use .firebaserc.example as the template)
.firebaserc

# Docs: outward-facing docs and planning docs are tracked; other internal working docs stay local
docs/*
!docs/architecture.md
!docs/plans/

# Dev gallery (gallery.html + src/dev/) is local dev tooling: not tracked, and not
# a production build input (only index.html is built). Run it via `npm run gallery`.
gallery.html
trials.html
src/dev/

# Cloud Functions build output
functions/lib
